import styled from "styled-components";

const Wrapper = styled.div`
  display: grid;
  place-items: center;
  height: 100vh;
  .form {
    height: 40rem;
    width: 30rem;
    /* background: blue; */
  }
`;

export default Wrapper;
